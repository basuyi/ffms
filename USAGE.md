# 🚀 快速使用指南

## 启动应用

```bash
# 1. 启动应用
./start.sh

# 2. 等待启动完成（大约30秒）

# 3. 测试应用状态
./test.sh

# 4. 在浏览器中访问
# http://localhost:3000
```

## 基本操作

### 1. 创建工作流 📝

1. **添加节点**
   - 点击右下角的 ➕ 按钮
   - 选择节点类型（开始、任务、条件等）
   - 节点会自动添加到画布

2. **连接节点**
   - 拖拽节点边缘的连接点
   - 连接到其他节点的输入点
   - 形成工作流程

3. **编辑节点属性**
   - 点击选中节点
   - 在右侧面板编辑属性
   - 配置节点参数

### 2. 保存工作流 💾

1. 点击顶部工具栏的 **保存** 按钮
2. 输入工作流名称和描述
3. 点击确认保存

### 3. 执行工作流 ⚡

1. 确保工作流已保存
2. 点击顶部工具栏的 **执行** 按钮
3. 观察节点状态变化
4. 查看执行结果

### 4. 管理工作流 📋

1. 点击顶部工具栏的 **打开** 按钮
2. 查看已保存的工作流列表
3. 选择工作流进行加载或删除

## 节点类型说明

| 节点类型 | 图标 | 说明 | 配置参数 |
|---------|------|------|----------|
| 开始 | 🟢 | 工作流起点 | 无 |
| 结束 | 🔴 | 工作流终点 | 无 |
| 任务 | 🔵 | 执行具体任务 | 任务标题、执行命令 |
| 条件判断 | 🟠 | 分支判断 | 条件表达式、操作符、比较值 |
| 并行处理 | 🟣 | 并行执行 | 无 |
| 合并 | ⚫ | 合并分支 | 无 |
| 延时 | 🟤 | 等待时间 | 延时时间(毫秒) |
| API调用 | 🔵 | 调用外部API | URL、HTTP方法、请求头、请求体 |

## 快速示例

### 创建简单工作流

1. **添加开始节点** → **添加任务节点** → **添加结束节点**
2. **连接节点**: 开始 → 任务 → 结束
3. **配置任务节点**: 
   - 任务标题: "Hello World"
   - 执行命令: "echo 'Hello, World!'"
4. **保存**: 名称为 "我的第一个工作流"
5. **执行**: 观察执行过程

### API 示例工作流

1. **开始** → **API调用** → **条件判断** → **任务处理** → **结束**
2. **配置API节点**:
   - URL: `https://jsonplaceholder.typicode.com/users`
   - 方法: GET
3. **配置条件节点**:
   - 条件: `response.length > 0`
4. **执行工作流**

## 快捷键

| 操作 | 快捷键 |
|------|--------|
| 添加节点 | 点击 ➕ 按钮 |
| 删除节点 | 选中节点 → 属性面板 → 删除按钮 |
| 保存工作流 | 顶部工具栏 → 保存 |
| 执行工作流 | 顶部工具栏 → 执行 |
| 缩放画布 | 鼠标滚轮 |
| 拖拽画布 | 鼠标拖拽空白区域 |

## 故障排除

### 应用无法访问
```bash
# 检查服务状态
./test.sh

# 重启应用
./stop.sh
./start.sh
```

### 节点无法连接
- 检查节点类型兼容性
- 确保连接方向正确（输出 → 输入）
- 开始节点只有输出，结束节点只有输入

### 工作流执行失败
- 检查节点配置是否正确
- 查看右下角执行状态面板
- 检查后端日志: `tail -f backend.log`

## 高级功能

### WebSocket 实时监控
- 工作流执行时可实时查看节点状态
- 节点会显示不同颜色表示状态：
  - 🟡 执行中
  - 🟢 已完成  
  - 🔴 执行失败

### API 集成
- 支持调用外部 REST API
- 可配置请求头和请求体
- 支持所有 HTTP 方法

### 条件分支
- 支持复杂的条件判断
- 可根据前置节点结果进行分支
- 支持多种比较操作符

## 停止应用

```bash
./stop.sh
```

---

📚 更多详细信息请查看 [README.md](README.md)